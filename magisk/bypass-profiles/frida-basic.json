{
  "name": "frida-basic",
  "description": "Runtime profile for Frida detection labs. It logs scope and performs no global hiding by default.",
  "scope": "target-package-runtime",
  "allowedUse": "Only CTF, lab, self-owned apps, or explicitly authorized testing. Never global device patching.",
  "protections": ["frida_detection"],
  "requiresConfirm": true,
  "actions": [
    {
      "type": "frida_script",
      "description": "Observation-only action.",
      "script": "log('frida-basic profile loaded; no global frida hiding is performed. Add target-specific app hooks only when authorized.');"
    }
  ],
  "revert": [
    {"type": "detach", "description": "Detach the Frida session or call ctf.revert_bypass_profile."}
  ]
}
